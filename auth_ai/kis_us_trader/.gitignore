# 프로젝트 로컬 ignore (부모 .gitignore는 .env/__pycache__/.venv 등을 잡아준다)

# 봇 영속 상태 — 절대 커밋 금지(주문 이력/누적 카운터 포함)
.state/

# 감사 로그 — 운영 산출물(원하면 별도 보관, 코드 리포지토리엔 넣지 않음)
logs/

# KIS 토큰 파일 캐시(전역 .gitignore에도 있지만 안전망)
.kis_us_trader/
token_*.json
